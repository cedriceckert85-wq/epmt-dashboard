# Skeleton only. Phase 00 must validate current Claude CLI flags via doctor/capability tests.
# Intended mode: non-interactive claude -p with structured JSON schema output.
# Reviewer must be read-only. Builder write permissions must be explicitly constrained.
