# Skeleton only. Phase 00 must validate current Codex CLI flags via doctor/capability tests.
# Intended mode: codex exec with output schema and explicit sandbox.
# Reviewer = read-only; Builder = workspace-write.
