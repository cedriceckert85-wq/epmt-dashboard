id: '18'
name: Multi-Streamer
builder: claude
reviewer: codex
cross_vendor_review: true
human_gate: true
required_tests:
- phase_18_tests
allowed_write_paths:
- src/
- tests/
- docs/
- reports/
gate:
  max_blockers: 0
  max_unaccepted_high: 0
optional: true
enabled_by_default: false
precondition_note: Enable only if user explicitly expands scope beyond one streamer.
metrics:
- name: cross_tenant_denied_pct
  op: ==
  value: 100
